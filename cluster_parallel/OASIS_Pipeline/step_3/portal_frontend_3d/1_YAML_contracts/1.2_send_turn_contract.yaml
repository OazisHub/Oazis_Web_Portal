schema: step_contract.v4
contract_meta:
  contract_version: "4.0.0"
  step_id: "1.2"
  step_name: "1.2_send_turn"
  python_min_version: "3.11"
execution:
  required_chain:
    - a_bootstrap
    - b_panel_locator
    - c_job_descriptor
    - d_job_path_resolver
paths:
  input:
    mode_enum: ["single", "batch", "stream"]
  job_root:
    must_contain_job_id_in_batch: true
output_artifacts:
  required_for_any_disk_write: true
meta_output:
  required: true
